name: allervie-dashboard
region: nyc
services:
  - name: web
    github:
      repo: jhillbht/allervie-dashboard
      branch: main
      deploy_on_push: true
    
    # Build configuration with npm version management
    build_command: |
      echo "Updating npm to version 10.4.0..."
      npm install -g npm@10.4.0
      echo "Installing dependencies..."
      if [ ! -f "package-lock.json" ]; then
        npm install
      else
        npm ci
      fi
      echo "Building application..."
      npm run build
    run_command: npm start
    
    # Instance configuration
    instance_count: 1
    instance_size_slug: basic-xxs
    http_port: 8080
    
    # Route configuration
    routes:
      - path: /
        preserve_path_prefix: true
    
    # Environment variables
    envs:
      - key: NODE_ENV
        scope: RUN_AND_BUILD_TIME
        value: production
      
      # Supabase configuration
      - key: VITE_SUPABASE_URL
        scope: BUILD_TIME
        type: SECRET
      - key: VITE_SUPABASE_ANON_KEY
        scope: BUILD_TIME
        type: SECRET
      
      # Google Analytics configuration
      - key: VITE_GA_MEASUREMENT_ID
        scope: BUILD_TIME
        type: SECRET
      
      # Google Ads configuration
      - key: VITE_GOOGLE_ADS_CONVERSION_IDS
        scope: BUILD_TIME
        type: SECRET
    
    # Health check configuration with adjusted timings
    health_check:
      http_path: /health
      initial_delay_seconds: 30
      period_seconds: 15
      timeout_seconds: 5
      success_threshold: 1
      failure_threshold: 3
    
    # CORS configuration
    cors:
      allow_origins:
        - "https://*.allervie.com"
      allow_methods:
        - GET
        - POST
        - PUT
        - DELETE
        - OPTIONS
      allow_headers:
        - Authorization
        - Content-Type
      max_age: "86400"
    
    # Alert configuration
    alert:
      rule: CPU_UTILIZATION
      value: 90
      operator: GREATER_THAN
      window: FIVE_MINUTES
      period: TWENTY_FOUR_HOURS